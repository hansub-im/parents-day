# /etc/parents-day/env  — chmod 600, owner parents-day:parents-day
# 이 파일을 서버에 복사하고 값 채워서 배치. 절대 git에 커밋 금지.

SPRING_PROFILES_ACTIVE=prod

# Cafe24 MSSQL
DB_URL=jdbc:sqlserver://sql16ssd-008.localnet.kr:1433;databaseName=<DB_NAME>;encrypt=true;trustServerCertificate=true;sendStringParametersAsUnicode=true
DB_USERNAME=<DB_USERNAME>
DB_PASSWORD=<DB_PASSWORD>

# 운영 도메인 (콤마로 다중)
APP_CORS_ORIGINS=https://im-hansub.co.kr,https://www.im-hansub.co.kr

# 관리자 화면/API 비밀번호
ADMIN_PASSWORD=<ADMIN_PASSWORD>
